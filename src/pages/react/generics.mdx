---
title: React Generics
tags: ['react', 'generics']
---

### `const` {'in'} `tsx` file

{/* prettier-ignore */}
```ts showLineNumbers
const foo = <T,>(value: T): T | null => {
  return value || null;
};
```
