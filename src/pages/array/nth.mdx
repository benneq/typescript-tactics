---
title: nth
tags: ['array', 'index', 'nth']
---

### nth

```json showLineNumbers
const nth = <T extends unknown[], I extends number = 0>(
  array: T,
  index: I = 0 as I
): T[I] => {
  return array.at(index);
};
```
